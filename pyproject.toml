[project]
name = "claude-superpowers"
version = "0.1.0"
description = "Autonomous skill execution, messaging, scheduling, and orchestration for Claude Code"
requires-python = ">=3.12"
license = {text = "MIT"}
authors = [{name = "SmartMur", email = "contact@smartmur.com"}]
dependencies = [
    "pyyaml>=6.0",
    "click>=8.1",
    "rich>=13.0",
    "cryptography>=46.0.7",
    "apscheduler>=3.10",
    "sqlalchemy>=2.0",
    "paramiko>=3.4",
    "watchdog>=4.0",
    "aiohttp>=3.14.0",
    "fastapi>=0.110",
    "PyJWT>=2.13.0",
    "uvicorn>=0.27",
    "redis>=5.0",
    "slack_sdk>=3.27",
    "playwright>=1.55.1",
    "httpx>=0.27",
    "mcp>=1.26.0",
    "anthropic>=0.39",
    "openai>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    # Pinned: ruff's formatter output changes between releases, so an unpinned
    # version makes `ruff format --check` in CI fail whenever a new ruff ships.
    "ruff==0.15.16",
]
docs = [
    "mkdocs-material>=9.5",
]

[project.scripts]
claw = "superpowers.cli:main"
claw-mcp = "superpowers.mcp_server:main"

[tool.setuptools.packages.find]
include = ["superpowers*"]

[build-system]
requires = ["setuptools>=69.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
    "slow: marks tests as slow (soak/reliability tests)",
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# Tests patch classes with PascalCase mock args (MockRM) and import skill
# modules after sys.path manipulation, which both rules legitimately flag.
"tests/**" = ["N803", "E402"]

[tool.ruff.lint.isort]
known-first-party = ["superpowers"]
