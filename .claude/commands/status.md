Show the claude-superpowers system status dashboard.

Run this command from the project root:

```bash
cd ~/Projects/claude-superpowers && python -m superpowers.cli status
```

This displays a Rich panel with the status of all subsystems: cron jobs, messaging channels, file watchers, skills, vault, memory, and recent audit log entries.
